 rotate the array by k
 
 approach->  arr[i+k]%n;
 cyclic shifting of k postions shifting of ith term with this->
 
 code->
 
  vector<int> temp(nums.size);
  loop->
  
  temp[i+k]%array.size()]=nums[i];
   nums=temp;
   
  
 
 
